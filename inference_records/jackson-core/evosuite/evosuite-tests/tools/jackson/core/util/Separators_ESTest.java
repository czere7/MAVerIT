/*
 * This file was automatically generated by EvoSuite
 * Mon Jul 27 15:03:18 GMT 2026
 */

package tools.jackson.core.util;

import org.junit.Test;
import static org.junit.Assert.*;
import org.evosuite.runtime.EvoRunner;
import org.evosuite.runtime.EvoRunnerParameters;
import org.junit.runner.RunWith;
import tools.jackson.core.util.Separators;

@RunWith(EvoRunner.class) @EvoRunnerParameters(mockJVMNonDeterminism = true, useVFS = true, useVNET = true, resetStaticState = true, separateClassLoader = true, useJEE = true) 
public class Separators_ESTest extends Separators_ESTest_scaffolding {

  @Test(timeout = 4000)
  public void test00()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.NONE;
      Separators separators0 = new Separators("", '', separators_Spacing0, '', separators_Spacing0, "", '', separators_Spacing0, (String) null);
      Separators separators1 = separators0.withArrayEmptySeparator(" ");
      assertEquals(" ", separators1.getArrayEmptySeparator());
      assertEquals('', separators0.getArrayElementSeparator());
      assertEquals("", separators1.getRootSeparator());
      assertEquals("", separators1.getObjectEmptySeparator());
      assertNotSame(separators1, separators0);
      assertEquals('', separators0.getObjectNameValueSeparator());
      assertEquals('', separators0.getObjectEntrySeparator());
  }

  @Test(timeout = 4000)
  public void test01()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.BOTH;
      Separators.Spacing separators_Spacing1 = Separators.Spacing.AFTER;
      Separators separators0 = new Separators(" ", 'I', separators_Spacing1, '2', separators_Spacing1, (String) null, 'I', separators_Spacing0, (String) null);
      Separators separators1 = separators0.withArrayElementSpacing(separators_Spacing1);
      assertEquals(Separators.Spacing.AFTER, separators0.getObjectEntrySpacing());
      assertEquals('I', separators0.getArrayElementSeparator());
      assertEquals('2', separators0.getObjectEntrySeparator());
      assertNotSame(separators1, separators0);
      assertEquals('I', separators0.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.AFTER, separators0.getObjectNameValueSpacing());
  }

  @Test(timeout = 4000)
  public void test02()  throws Throwable  {
      Separators separators0 = new Separators('t', 'x', 'x');
      Separators separators1 = separators0.withArrayEmptySeparator("35");
      Separators separators2 = separators1.withArrayElementSeparator('t');
      assertEquals(" ", separators2.getRootSeparator());
      assertEquals(" ", separators2.getObjectEmptySeparator());
      assertEquals('t', separators2.getArrayElementSeparator());
      assertNotSame(separators2, separators1);
      assertEquals('t', separators0.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.NONE, separators2.getArrayElementSpacing());
      assertEquals("35", separators2.getArrayEmptySeparator());
      assertEquals(Separators.Spacing.NONE, separators2.getObjectEntrySpacing());
      assertEquals(Separators.Spacing.BOTH, separators2.getObjectNameValueSpacing());
      assertEquals('x', separators2.getObjectEntrySeparator());
  }

  @Test(timeout = 4000)
  public void test03()  throws Throwable  {
      Separators separators0 = Separators.createDefaultInstance();
      Separators separators1 = separators0.withObjectEmptySeparator("q:/p}C<n}vc6<%6V");
      Separators separators2 = separators1.withArrayElementSeparator('5');
      assertEquals(',', separators2.getObjectEntrySeparator());
      assertEquals(':', separators2.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.NONE, separators2.getObjectEntrySpacing());
      assertEquals(Separators.Spacing.BOTH, separators2.getObjectNameValueSpacing());
      assertEquals(" ", separators2.getRootSeparator());
      assertEquals("q:/p}C<n}vc6<%6V", separators2.getObjectEmptySeparator());
      assertEquals(Separators.Spacing.NONE, separators2.getArrayElementSpacing());
      assertEquals(" ", separators2.getArrayEmptySeparator());
      assertEquals('5', separators2.getArrayElementSeparator());
  }

  @Test(timeout = 4000)
  public void test04()  throws Throwable  {
      Separators separators0 = Separators.createDefaultInstance();
      Separators separators1 = separators0.withRootSeparator("Z1Q0jtot6");
      Separators separators2 = separators1.withArrayElementSeparator('5');
      assertEquals("Z1Q0jtot6", separators2.getRootSeparator());
      assertEquals(" ", separators2.getObjectEmptySeparator());
      assertEquals(" ", separators2.getArrayEmptySeparator());
      assertEquals(Separators.Spacing.NONE, separators2.getArrayElementSpacing());
      assertEquals('5', separators2.getArrayElementSeparator());
      assertEquals(Separators.Spacing.BOTH, separators2.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators2.getObjectEntrySpacing());
      assertEquals(':', separators2.getObjectNameValueSeparator());
      assertEquals(',', separators2.getObjectEntrySeparator());
  }

  @Test(timeout = 4000)
  public void test05()  throws Throwable  {
      Separators separators0 = new Separators('t', 'x', 'x');
      Separators separators1 = separators0.withArrayEmptySeparator("35");
      Separators separators2 = separators1.withObjectEmptySeparator("");
      assertEquals(" ", separators2.getRootSeparator());
      assertEquals('x', separators2.getArrayElementSeparator());
      assertEquals(Separators.Spacing.NONE, separators2.getArrayElementSpacing());
      assertEquals('t', separators2.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.BOTH, separators2.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators2.getObjectEntrySpacing());
      assertEquals("35", separators2.getArrayEmptySeparator());
      assertEquals('x', separators2.getObjectEntrySeparator());
      assertEquals("", separators2.getObjectEmptySeparator());
  }

  @Test(timeout = 4000)
  public void test06()  throws Throwable  {
      Separators separators0 = Separators.createDefaultInstance();
      Separators separators1 = separators0.withRootSeparator("Z1Q0jtot6");
      Separators separators2 = separators1.withObjectEmptySeparator("q:/p}C<n}vc6<%6V");
      assertEquals(" ", separators2.getArrayEmptySeparator());
      assertEquals(Separators.Spacing.BOTH, separators2.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators2.getObjectEntrySpacing());
      assertEquals(',', separators2.getObjectEntrySeparator());
      assertEquals("q:/p}C<n}vc6<%6V", separators2.getObjectEmptySeparator());
      assertEquals(':', separators2.getObjectNameValueSeparator());
      assertEquals("Z1Q0jtot6", separators2.getRootSeparator());
      assertEquals(',', separators2.getArrayElementSeparator());
      assertEquals(Separators.Spacing.NONE, separators2.getArrayElementSpacing());
  }

  @Test(timeout = 4000)
  public void test07()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.AFTER;
      Separators separators0 = new Separators("", '', separators_Spacing0, '', separators_Spacing0, "", '', separators_Spacing0, "");
      Separators separators1 = separators0.withObjectEmptySeparator("");
      assertEquals("", separators1.getArrayEmptySeparator());
      assertSame(separators1, separators0);
      assertEquals('', separators1.getObjectNameValueSeparator());
      assertEquals('', separators1.getObjectEntrySeparator());
      assertEquals("", separators1.getRootSeparator());
      assertEquals('', separators1.getArrayElementSeparator());
  }

  @Test(timeout = 4000)
  public void test08()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.NONE;
      Separators separators0 = new Separators("", '9', separators_Spacing0, 'F', separators_Spacing0, "NVlL", 'z', separators_Spacing0, (String) null);
      Separators.Spacing separators_Spacing1 = Separators.Spacing.BOTH;
      Separators separators1 = separators0.withObjectEntrySpacing(separators_Spacing1);
      assertEquals("", separators1.getRootSeparator());
      assertEquals('z', separators0.getArrayElementSeparator());
      assertEquals('F', separators0.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectNameValueSpacing());
      assertEquals("NVlL", separators1.getObjectEmptySeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
      assertEquals(Separators.Spacing.BOTH, separators1.getObjectEntrySpacing());
      assertEquals('9', separators0.getObjectNameValueSeparator());
  }

  @Test(timeout = 4000)
  public void test09()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.NONE;
      Separators separators0 = new Separators("", '', separators_Spacing0, '', separators_Spacing0, "", '', separators_Spacing0, (String) null);
      Separators separators1 = separators0.withObjectEntrySeparator('L');
      assertEquals('L', separators1.getObjectEntrySeparator());
      assertEquals("", separators1.getObjectEmptySeparator());
      assertEquals('', separators0.getObjectNameValueSeparator());
      assertEquals('', separators1.getObjectNameValueSeparator());
      assertEquals("", separators1.getRootSeparator());
      assertEquals('', separators0.getArrayElementSeparator());
      assertEquals('', separators1.getArrayElementSeparator());
  }

  @Test(timeout = 4000)
  public void test10()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.NONE;
      Separators separators0 = new Separators("", '', separators_Spacing0, '', separators_Spacing0, "", '', separators_Spacing0, (String) null);
      Separators.Spacing separators_Spacing1 = Separators.Spacing.BEFORE;
      Separators separators1 = separators0.withObjectNameValueSpacing(separators_Spacing1);
      assertEquals('', separators0.getObjectNameValueSeparator());
      assertEquals('', separators0.getObjectEntrySeparator());
      assertEquals("", separators1.getRootSeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectEntrySpacing());
      assertEquals('', separators0.getArrayElementSeparator());
      assertEquals("", separators1.getObjectEmptySeparator());
      assertEquals(Separators.Spacing.BEFORE, separators1.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
  }

  @Test(timeout = 4000)
  public void test11()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.NONE;
      Separators separators0 = new Separators("", '', separators_Spacing0, '', separators_Spacing0, "", '', separators_Spacing0, (String) null);
      Separators separators1 = separators0.withObjectNameValueSeparator('A');
      assertEquals('A', separators1.getObjectNameValueSeparator());
      assertEquals('', separators0.getObjectEntrySeparator());
      assertEquals("", separators1.getObjectEmptySeparator());
      assertEquals('', separators0.getArrayElementSeparator());
      assertEquals('', separators1.getObjectEntrySeparator());
      assertEquals("", separators1.getRootSeparator());
      assertEquals('', separators1.getArrayElementSeparator());
  }

  @Test(timeout = 4000)
  public void test12()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.BEFORE;
      Separators separators0 = new Separators((String) null, '@', separators_Spacing0, '-', separators_Spacing0, "/F0JKphoe$", '@', separators_Spacing0, "r7");
      Separators separators1 = separators0.withRootSeparator("");
      assertEquals('@', separators0.getObjectNameValueSeparator());
      assertEquals("", separators1.getRootSeparator());
      assertEquals("r7", separators1.getArrayEmptySeparator());
      assertNotSame(separators1, separators0);
      assertEquals('@', separators0.getArrayElementSeparator());
      assertEquals("/F0JKphoe$", separators1.getObjectEmptySeparator());
      assertEquals('-', separators0.getObjectEntrySeparator());
  }

  @Test(timeout = 4000)
  public void test13()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.NONE;
      Separators separators0 = new Separators("", '9', separators_Spacing0, 'F', separators_Spacing0, "NVlL", 'z', separators_Spacing0, (String) null);
      Separators separators1 = separators0.withRootSeparator("");
      assertEquals('z', separators1.getArrayElementSeparator());
      assertEquals("NVlL", separators1.getObjectEmptySeparator());
      assertEquals('F', separators1.getObjectEntrySeparator());
      assertEquals('9', separators1.getObjectNameValueSeparator());
      assertSame(separators1, separators0);
  }

  @Test(timeout = 4000)
  public void test14()  throws Throwable  {
      Separators separators0 = Separators.createDefaultInstance();
      Separators separators1 = separators0.withRootSeparator((String) null);
      String string0 = separators1.getRootSeparator();
      assertEquals(':', separators0.getObjectNameValueSeparator());
      assertEquals(',', separators0.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.BOTH, separators1.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectEntrySpacing());
      assertNull(string0);
      assertEquals(',', separators0.getArrayElementSeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
  }

  @Test(timeout = 4000)
  public void test15()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.NONE;
      Separators separators0 = new Separators("rapKb;C?-.C0OA6", '3', separators_Spacing0, '3', separators_Spacing0, "$VALUES", 'j', separators_Spacing0, "tools.jackson.core.util.Separators");
      char char0 = separators0.getObjectNameValueSeparator();
      assertEquals('3', char0);
      assertEquals("tools.jackson.core.util.Separators", separators0.getArrayEmptySeparator());
      assertEquals("$VALUES", separators0.getObjectEmptySeparator());
      assertEquals("rapKb;C?-.C0OA6", separators0.getRootSeparator());
      assertEquals('3', separators0.getObjectEntrySeparator());
      assertEquals('j', separators0.getArrayElementSeparator());
  }

  @Test(timeout = 4000)
  public void test16()  throws Throwable  {
      Separators separators0 = Separators.createDefaultInstance();
      Separators separators1 = separators0.withObjectNameValueSeparator('P');
      char char0 = separators1.getObjectNameValueSeparator();
      assertEquals(',', separators0.getArrayElementSeparator());
      assertEquals(',', separators0.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
      assertEquals(Separators.Spacing.BOTH, separators1.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectEntrySpacing());
      assertEquals('P', char0);
  }

  @Test(timeout = 4000)
  public void test17()  throws Throwable  {
      Separators separators0 = new Separators('K', 'K', 'K');
      Separators separators1 = separators0.withObjectEntrySpacing((Separators.Spacing) null);
      separators1.getObjectEntrySpacing();
      assertEquals(Separators.Spacing.BOTH, separators0.getObjectNameValueSpacing());
      assertEquals('K', separators0.getArrayElementSeparator());
      assertNotSame(separators1, separators0);
      assertEquals('K', separators0.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
      assertEquals(Separators.Spacing.NONE, separators0.getObjectEntrySpacing());
      assertEquals('K', separators0.getObjectNameValueSeparator());
  }

  @Test(timeout = 4000)
  public void test18()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.BOTH;
      Separators separators0 = new Separators(" ", 'I', separators_Spacing0, '2', separators_Spacing0, (String) null, 'I', separators_Spacing0, (String) null);
      char char0 = separators0.getObjectEntrySeparator();
      assertEquals('2', char0);
      assertEquals('I', separators0.getArrayElementSeparator());
      assertEquals('I', separators0.getObjectNameValueSeparator());
  }

  @Test(timeout = 4000)
  public void test19()  throws Throwable  {
      Separators separators0 = new Separators('Q', 'u', 'Q');
      char char0 = separators0.getObjectEntrySeparator();
      assertEquals('Q', separators0.getArrayElementSeparator());
      assertEquals('u', char0);
      assertEquals(Separators.Spacing.NONE, separators0.getArrayElementSpacing());
      assertEquals(Separators.Spacing.BOTH, separators0.getObjectNameValueSpacing());
      assertEquals('Q', separators0.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.NONE, separators0.getObjectEntrySpacing());
  }

  @Test(timeout = 4000)
  public void test20()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.BOTH;
      Separators separators0 = new Separators(" ", 'I', separators_Spacing0, '2', separators_Spacing0, (String) null, 'I', separators_Spacing0, (String) null);
      String string0 = separators0.getObjectEmptySeparator();
      assertEquals('I', separators0.getObjectNameValueSeparator());
      assertNull(string0);
      assertEquals('2', separators0.getObjectEntrySeparator());
      assertEquals('I', separators0.getArrayElementSeparator());
  }

  @Test(timeout = 4000)
  public void test21()  throws Throwable  {
      Separators separators0 = new Separators('~', '~', 'Y');
      Separators separators1 = separators0.withObjectEmptySeparator("");
      String string0 = separators1.getObjectEmptySeparator();
      assertEquals('~', separators0.getObjectEntrySeparator());
      assertEquals(" ", separators1.getRootSeparator());
      assertEquals('Y', separators0.getArrayElementSeparator());
      assertEquals('~', separators0.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectEntrySpacing());
      assertEquals(Separators.Spacing.BOTH, separators1.getObjectNameValueSpacing());
      assertEquals("", string0);
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
      assertEquals(" ", separators1.getArrayEmptySeparator());
  }

  @Test(timeout = 4000)
  public void test22()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.NONE;
      Separators separators0 = new Separators("", '9', separators_Spacing0, 'F', separators_Spacing0, "NVlL", 'z', separators_Spacing0, (String) null);
      String string0 = separators0.getArrayEmptySeparator();
      assertNull(string0);
      assertEquals('9', separators0.getObjectNameValueSeparator());
      assertEquals("", separators0.getRootSeparator());
      assertEquals("NVlL", separators0.getObjectEmptySeparator());
      assertEquals('z', separators0.getArrayElementSeparator());
      assertEquals('F', separators0.getObjectEntrySeparator());
  }

  @Test(timeout = 4000)
  public void test23()  throws Throwable  {
      Separators separators0 = new Separators('S', 'e', 'e');
      Separators separators1 = separators0.withArrayEmptySeparator("");
      String string0 = separators1.getArrayEmptySeparator();
      assertEquals('e', separators0.getObjectEntrySeparator());
      assertEquals(" ", separators1.getRootSeparator());
      assertEquals('e', separators0.getArrayElementSeparator());
      assertEquals('S', separators0.getObjectNameValueSeparator());
      assertEquals("", string0);
      assertEquals(Separators.Spacing.BOTH, separators1.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectEntrySpacing());
      assertEquals(" ", separators1.getObjectEmptySeparator());
  }

  @Test(timeout = 4000)
  public void test24()  throws Throwable  {
      Separators separators0 = new Separators('5', '5', '5');
      char char0 = separators0.getArrayElementSeparator();
      assertEquals(Separators.Spacing.NONE, separators0.getArrayElementSpacing());
      assertEquals('5', separators0.getObjectEntrySeparator());
      assertEquals('5', char0);
      assertEquals('5', separators0.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.BOTH, separators0.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators0.getObjectEntrySpacing());
  }

  @Test(timeout = 4000)
  public void test25()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.BOTH;
      Separators separators0 = new Separators("fLV>,Hu#8", 'N', separators_Spacing0, 'N', separators_Spacing0, "fLV>,Hu#8", 'h', separators_Spacing0, "fLV>,Hu#8");
      char char0 = separators0.getArrayElementSeparator();
      assertEquals('N', separators0.getObjectEntrySeparator());
      assertEquals("fLV>,Hu#8", separators0.getRootSeparator());
      assertEquals("fLV>,Hu#8", separators0.getArrayEmptySeparator());
      assertEquals('h', char0);
      assertEquals('N', separators0.getObjectNameValueSeparator());
      assertEquals("fLV>,Hu#8", separators0.getObjectEmptySeparator());
  }

  @Test(timeout = 4000)
  public void test26()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.NONE;
      Separators separators0 = new Separators("", '', separators_Spacing0, '', separators_Spacing0, "", '', separators_Spacing0, (String) null);
      String string0 = separators0.getRootSeparator();
      assertEquals("", string0);
      assertEquals("", separators0.getObjectEmptySeparator());
      assertEquals('', separators0.getArrayElementSeparator());
      assertEquals('', separators0.getObjectEntrySeparator());
      assertEquals('', separators0.getObjectNameValueSeparator());
      assertNotNull(string0);
  }

  @Test(timeout = 4000)
  public void test27()  throws Throwable  {
      Separators separators0 = Separators.createDefaultInstance();
      Separators separators1 = separators0.withArrayEmptySeparator(" ");
      assertEquals(',', separators1.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectEntrySpacing());
      assertSame(separators1, separators0);
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
      assertEquals(',', separators1.getArrayElementSeparator());
      assertEquals(Separators.Spacing.BOTH, separators1.getObjectNameValueSpacing());
      assertEquals(':', separators1.getObjectNameValueSeparator());
  }

  @Test(timeout = 4000)
  public void test28()  throws Throwable  {
      Separators separators0 = new Separators();
      Separators separators1 = separators0.withArrayEmptySeparator("BEFORE");
      assertEquals(" ", separators1.getRootSeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectEntrySpacing());
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
      assertEquals(':', separators0.getObjectNameValueSeparator());
      assertEquals(',', separators0.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.BOTH, separators1.getObjectNameValueSpacing());
      assertEquals("BEFORE", separators1.getArrayEmptySeparator());
      assertEquals(',', separators0.getArrayElementSeparator());
      assertEquals(" ", separators1.getObjectEmptySeparator());
  }

  @Test(timeout = 4000)
  public void test29()  throws Throwable  {
      Separators separators0 = Separators.createDefaultInstance();
      Separators.Spacing separators_Spacing0 = Separators.Spacing.AFTER;
      Separators separators1 = separators0.withArrayElementSpacing(separators_Spacing0);
      Separators separators2 = separators1.withArrayElementSpacing(separators_Spacing0);
      assertEquals(':', separators0.getObjectNameValueSeparator());
      assertEquals(',', separators0.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.NONE, separators2.getObjectEntrySpacing());
      assertEquals(Separators.Spacing.BOTH, separators2.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.AFTER, separators2.getArrayElementSpacing());
      assertEquals(',', separators0.getArrayElementSeparator());
  }

  @Test(timeout = 4000)
  public void test30()  throws Throwable  {
      Separators separators0 = new Separators('K', 'K', 'K');
      Separators separators1 = separators0.withArrayElementSeparator('K');
      assertSame(separators1, separators0);
      assertEquals('K', separators1.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectEntrySpacing());
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
      assertEquals('K', separators1.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.BOTH, separators1.getObjectNameValueSpacing());
  }

  @Test(timeout = 4000)
  public void test31()  throws Throwable  {
      Separators separators0 = new Separators();
      Separators.Spacing separators_Spacing0 = Separators.Spacing.NONE;
      Separators separators1 = separators0.withObjectEntrySpacing(separators_Spacing0);
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
      assertEquals(',', separators1.getArrayElementSeparator());
      assertSame(separators1, separators0);
      assertEquals(Separators.Spacing.BOTH, separators1.getObjectNameValueSpacing());
      assertEquals(',', separators1.getObjectEntrySeparator());
      assertEquals(':', separators1.getObjectNameValueSeparator());
  }

  @Test(timeout = 4000)
  public void test32()  throws Throwable  {
      Separators separators0 = new Separators('K', 'K', 'K');
      Separators separators1 = separators0.withObjectEntrySeparator('K');
      assertEquals('K', separators1.getObjectNameValueSeparator());
      assertSame(separators1, separators0);
      assertEquals(Separators.Spacing.BOTH, separators1.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectEntrySpacing());
      assertEquals('K', separators1.getArrayElementSeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
  }

  @Test(timeout = 4000)
  public void test33()  throws Throwable  {
      Separators separators0 = new Separators();
      Separators separators1 = separators0.withObjectEntrySeparator(':');
      assertEquals(':', separators0.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.BOTH, separators1.getObjectNameValueSpacing());
      assertEquals(':', separators1.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectEntrySpacing());
      assertEquals(',', separators1.getArrayElementSeparator());
      assertEquals(',', separators0.getArrayElementSeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
      assertNotSame(separators1, separators0);
  }

  @Test(timeout = 4000)
  public void test34()  throws Throwable  {
      Separators separators0 = new Separators();
      Separators.Spacing separators_Spacing0 = Separators.Spacing.BOTH;
      Separators separators1 = separators0.withObjectNameValueSpacing(separators_Spacing0);
      assertSame(separators1, separators0);
      assertEquals(':', separators1.getObjectNameValueSeparator());
      assertEquals(',', separators1.getArrayElementSeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectEntrySpacing());
      assertEquals(',', separators1.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
  }

  @Test(timeout = 4000)
  public void test35()  throws Throwable  {
      Separators separators0 = new Separators();
      Separators separators1 = separators0.withObjectNameValueSeparator(':');
      assertSame(separators1, separators0);
      assertEquals(',', separators1.getArrayElementSeparator());
      assertEquals(Separators.Spacing.NONE, separators1.getObjectEntrySpacing());
      assertEquals(Separators.Spacing.NONE, separators1.getArrayElementSpacing());
      assertEquals(Separators.Spacing.BOTH, separators1.getObjectNameValueSpacing());
      assertEquals(',', separators1.getObjectEntrySeparator());
  }

  @Test(timeout = 4000)
  public void test36()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.NONE;
      String string0 = separators_Spacing0.spacesBefore();
      assertEquals("", string0);
  }

  @Test(timeout = 4000)
  public void test37()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.BOTH;
      String string0 = separators_Spacing0.apply('y');
      assertEquals(" y ", string0);
  }

  @Test(timeout = 4000)
  public void test38()  throws Throwable  {
      Separators.Spacing separators_Spacing0 = Separators.Spacing.AFTER;
      String string0 = separators_Spacing0.spacesAfter();
      assertEquals(" ", string0);
  }

  @Test(timeout = 4000)
  public void test39()  throws Throwable  {
      Separators separators0 = new Separators();
      char char0 = separators0.getObjectNameValueSeparator();
      assertEquals(Separators.Spacing.BOTH, separators0.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators0.getObjectEntrySpacing());
      assertEquals(':', char0);
      assertEquals(',', separators0.getObjectEntrySeparator());
      assertEquals(',', separators0.getArrayElementSeparator());
      assertEquals(Separators.Spacing.NONE, separators0.getArrayElementSpacing());
  }

  @Test(timeout = 4000)
  public void test40()  throws Throwable  {
      Separators separators0 = new Separators();
      Separators.Spacing separators_Spacing0 = separators0.getArrayElementSpacing();
      assertEquals(':', separators0.getObjectNameValueSeparator());
      assertEquals(',', separators0.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.NONE, separators0.getObjectEntrySpacing());
      assertEquals(Separators.Spacing.BOTH, separators0.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators_Spacing0);
      assertEquals(',', separators0.getArrayElementSeparator());
  }

  @Test(timeout = 4000)
  public void test41()  throws Throwable  {
      Separators separators0 = new Separators('K', 'K', 'K');
      separators0.getObjectEmptySeparator();
      assertEquals('K', separators0.getArrayElementSeparator());
      assertEquals(Separators.Spacing.NONE, separators0.getArrayElementSpacing());
      assertEquals(Separators.Spacing.NONE, separators0.getObjectEntrySpacing());
      assertEquals('K', separators0.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.BOTH, separators0.getObjectNameValueSpacing());
      assertEquals('K', separators0.getObjectEntrySeparator());
  }

  @Test(timeout = 4000)
  public void test42()  throws Throwable  {
      Separators separators0 = Separators.createDefaultInstance();
      char char0 = separators0.getArrayElementSeparator();
      assertEquals(':', separators0.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.BOTH, separators0.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators0.getObjectEntrySpacing());
      assertEquals(',', char0);
      assertEquals(',', separators0.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.NONE, separators0.getArrayElementSpacing());
  }

  @Test(timeout = 4000)
  public void test43()  throws Throwable  {
      Separators separators0 = new Separators();
      char char0 = separators0.getObjectEntrySeparator();
      assertEquals(',', separators0.getArrayElementSeparator());
      assertEquals(Separators.Spacing.NONE, separators0.getArrayElementSpacing());
      assertEquals(',', char0);
      assertEquals(':', separators0.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.BOTH, separators0.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators0.getObjectEntrySpacing());
  }

  @Test(timeout = 4000)
  public void test44()  throws Throwable  {
      Separators separators0 = new Separators('K', 'K', 'K');
      separators0.getRootSeparator();
      assertEquals('K', separators0.getArrayElementSeparator());
      assertEquals(Separators.Spacing.NONE, separators0.getObjectEntrySpacing());
      assertEquals('K', separators0.getObjectEntrySeparator());
      assertEquals('K', separators0.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.BOTH, separators0.getObjectNameValueSpacing());
      assertEquals(Separators.Spacing.NONE, separators0.getArrayElementSpacing());
  }

  @Test(timeout = 4000)
  public void test45()  throws Throwable  {
      Separators separators0 = new Separators();
      Separators.Spacing separators_Spacing0 = separators0.getObjectNameValueSpacing();
      assertEquals(Separators.Spacing.NONE, separators0.getArrayElementSpacing());
      assertEquals(Separators.Spacing.BOTH, separators_Spacing0);
      assertEquals(Separators.Spacing.NONE, separators0.getObjectEntrySpacing());
      assertEquals(',', separators0.getObjectEntrySeparator());
      assertEquals(':', separators0.getObjectNameValueSeparator());
      assertEquals(',', separators0.getArrayElementSeparator());
  }

  @Test(timeout = 4000)
  public void test46()  throws Throwable  {
      Separators separators0 = new Separators('K', 'K', 'K');
      separators0.getArrayEmptySeparator();
      assertEquals('K', separators0.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.BOTH, separators0.getObjectNameValueSpacing());
      assertEquals('K', separators0.getObjectEntrySeparator());
      assertEquals(Separators.Spacing.NONE, separators0.getObjectEntrySpacing());
      assertEquals(Separators.Spacing.NONE, separators0.getArrayElementSpacing());
      assertEquals('K', separators0.getArrayElementSeparator());
  }

  @Test(timeout = 4000)
  public void test47()  throws Throwable  {
      Separators separators0 = new Separators('K', 'K', 'K');
      Separators.Spacing separators_Spacing0 = separators0.getObjectEntrySpacing();
      assertEquals('K', separators0.getArrayElementSeparator());
      assertEquals(Separators.Spacing.BOTH, separators0.getObjectNameValueSpacing());
      assertEquals('K', separators0.getObjectEntrySeparator());
      assertEquals('K', separators0.getObjectNameValueSeparator());
      assertEquals(Separators.Spacing.NONE, separators_Spacing0);
      assertEquals(Separators.Spacing.NONE, separators0.getArrayElementSpacing());
  }
}
